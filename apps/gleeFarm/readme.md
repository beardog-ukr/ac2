This application converts "old" json files to "new" format. Old format is the same that is used by court websites (it's also called "raw"). The "new" one is the one used by current version of allcourts.tk.
